# Optional HTTPS server endpoint that accepts the JSON contract in README.md.
# Leave empty for a fully local, zero-AI-cost demonstration.
# Never put provider API secrets in VITE_* variables: they are public in the browser.
VITE_DIAGNOSIS_ENDPOINT=
